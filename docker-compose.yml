services:
  angular-escomio-app:
    container_name: angular-escomio-container_name
    build: .
    image: angular-escomio:v1
    ports:
      - 4200:80
    expose:
      - 4200
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge